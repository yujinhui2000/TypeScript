export enum NumType {
  One = 1,
  Two = 2,
  Three,
  Zero = 0,
  Four = One + Three,
  Five = Four + 1,
  LessThanSix = Zero | One | Two | Three | Four | Five,
}

export enum StrType {
  One = 'One',
  Two = 'Two',
}


